# API server
PORT=3001
NODE_ENV=development

# Staff authentication bootstrap
ADMIN_EMAIL=admin@your-university.edu
ADMIN_PASSWORD=replace-with-a-long-local-password
ADMIN_PASSWORD_SALT=replace-with-a-random-salt

# Browser access
WEB_ORIGIN=http://localhost:5173

# Required when the database-backed modules are used
# DATABASE_URL=postgresql://user:password@localhost:5432/mindful_campus
